import {  Component, Input, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'fontSize',
  template: `
  
  `,
  styleUrls: ['./font-size.css']
})
export class FontSizeComponent {
 
}
